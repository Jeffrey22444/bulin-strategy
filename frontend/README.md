# BBMR Local Dashboard

This frontend renders the local, read-only BBMR dashboard. It proxies only the
loopback dashboard API and does not start, stop, or control the trading runner.

Commands:

```bash
npm run dev
npm test
npm run lint
```
